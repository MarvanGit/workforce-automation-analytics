# Frontend

Angular dashboard shell for the workforce analytics and scheduling platform.

## Commands

Install dependencies:

```powershell
npm install
```

Run locally:

```powershell
npm run start
```

Build:

```powershell
npm run build
```

Run tests:

```powershell
npm run test
```

